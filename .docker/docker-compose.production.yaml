name: WESTWISE_PRODUCTION

x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  app:
    build:
      context: ..
      dockerfile: .docker/Dockerfile.production
    image: westwise:${APP_VERSION:-production}
    environment:
      DJANGO_SETTINGS_MODULE: core.settings
      DEBUG: "False"
      DATABASE_ENGINE: postgres
      SECRET_KEY: ${SECRET_KEY}
      OMIE_CREDENTIALS_ENCRYPTION_KEY: ${OMIE_CREDENTIALS_ENCRYPTION_KEY}
      ALLOWED_HOSTS: ${ALLOWED_HOSTS}
      CSRF_TRUSTED_ORIGINS: ${CSRF_TRUSTED_ORIGINS}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      SECURE_SSL_REDIRECT: ${SECURE_SSL_REDIRECT:-True}
      SESSION_COOKIE_SECURE: ${SESSION_COOKIE_SECURE:-True}
      CSRF_COOKIE_SECURE: ${CSRF_COOKIE_SECURE:-True}
    volumes:
      - WESTWISE_STATIC:/app/staticfiles
      - WESTWISE_MEDIA:/app/media
    expose:
      - "8000"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/healthz', timeout=5).read()\""]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    restart: unless-stopped
    networks:
      - WESTWISE_NETWORK
    logging: *default-logging

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - WESTWISE_POSTGRES_DATA:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    restart: unless-stopped
    networks:
      - WESTWISE_NETWORK
    logging: *default-logging

  proxy:
    image: nginx:alpine
    environment:
      DOMAIN: ${DOMAIN}
      PROXY_CLIENT_MAX_BODY_SIZE: ${PROXY_CLIENT_MAX_BODY_SIZE:-50M}
    ports:
      - "${PROXY_HTTP_PORT:-80}:80"
      - "${PROXY_HTTPS_PORT:-443}:443"
    volumes:
      - ./nginx.active.conf:/etc/nginx/templates/default.conf.template:ro
      - WESTWISE_CERTBOT_WWW:/var/www/certbot
      - WESTWISE_CERTBOT_CONF:/etc/letsencrypt
      - WESTWISE_STATIC:/app/staticfiles:ro
      - WESTWISE_MEDIA:/app/media:ro
    depends_on:
      app:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1/healthz >/dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    restart: unless-stopped
    networks:
      - WESTWISE_NETWORK
    logging: *default-logging

  certbot:
    image: certbot/certbot
    volumes:
      - WESTWISE_CERTBOT_WWW:/var/www/certbot
      - WESTWISE_CERTBOT_CONF:/etc/letsencrypt
    networks:
      - WESTWISE_NETWORK
    logging: *default-logging

volumes:
  WESTWISE_POSTGRES_DATA:
    name: WESTWISE_POSTGRES_DATA
  WESTWISE_STATIC:
    name: WESTWISE_STATIC
  WESTWISE_MEDIA:
    name: WESTWISE_MEDIA
  WESTWISE_CERTBOT_WWW:
    name: WESTWISE_CERTBOT_WWW
  WESTWISE_CERTBOT_CONF:
    name: WESTWISE_CERTBOT_CONF

networks:
  WESTWISE_NETWORK:
    name: WESTWISE_NETWORK
